framework:
    notifier:
        chatter_transports:
            discord: '%env(DISCORD_WEBHOOK_URL)%'
        texter_transports:
        channel_policy:
            urgent: ['discord', 'email']
            high: ['discord', 'email']
            medium: ['email']
            low: ['email']
        admin_recipients:
            - { email: admin@example.com }